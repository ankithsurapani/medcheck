# Source PDFs — large, re-fetchable with `python src/fetch.py --all`
data/pdfs/*.pdf

# Phase 0 throwaway dumps
data/raw/peek_*.txt
data/raw/profile.log

# Database
data/medcheck.db
*.db-journal

# Phase 2a working queue — 7.9 MB, and fully derived from medcheck.db by
# `python src/resolve/manufacturers.py --build`. The merge log next to it is the
# audit trail and IS committed; pair_ids hash the raw strings, so decisions
# recorded against one build are still found by the next one.
data/resolve/candidates.json

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node (web/)
node_modules/
.next/
web/tsconfig.tsbuildinfo
.env
.env.local

# OS
.DS_Store
